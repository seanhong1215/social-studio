{
  "name": "social-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:worker": "npm run db:migrate:local && wrangler dev --port 8790 --var APP_ENV:development --var AI_PROVIDER:demo",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "demo:record": "playwright test tests/e2e/record-user-flow.spec.ts",
    "e2e:prepare": "node scripts/prepare-e2e.mjs",
    "e2e:serve": "npm run build && npm run e2e:prepare && node scripts/serve-e2e.mjs",
    "deploy": "npm run build && wrangler deploy",
    "cf:typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply social-studio-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply social-studio-db --remote"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.9.6",
    "@lucide/vue": "^1.37.0",
    "vue": "^3.5.21",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250831.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.33.2"
  }
}
